import numpy as np

# Create a NumPy array with 10 random numbers
arr = np.random.rand(10)

# Perform basic operations
print("Array:", arr)
print("Sum:", np.sum(arr))
print("Mean:", np.mean(arr))
print("Max:", np.max(arr))
print("Min:", np.min(arr))
